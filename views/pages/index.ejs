<!DOCTYPE html>
<html lang="en">

<%- include("../partials/head"); %>          <!-- Inclui um EJS externo -->

<body class="container">
    <%- include("../partials/header"); %>

    <main>
        <div class="jumbotron">
            <h1>Node com EJS</h1>
            <p>Vamos aprender a trabalhar com arquivos EJS</p>
            <%= subtitle %>         <!-- Incluindo objeto passado pelo javascrpit -->
        </div>

        <ul>
            <% qualitys.forEach(function(quality){ %>           <!-- Codigo javascript deve estar entre as tags e HTML fora -->
                <li><strong><%= quality.title %></strong><%= quality.message %></li>
            <% }); %>
        </ul>
    </main>

    <%- include("../partials/footer"); %>
</body>
</html>